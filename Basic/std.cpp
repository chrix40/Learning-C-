#include <iostream>
#include <string>

int main() { 
  std::string hello = "Hello World!";
  std::cout << hello << std::endl;
  return 0; //  It is common to see some programs running without the standard library
            // in this case it can be changed by the keyword st, followed by the operator :: for string (and cout) objects:
}